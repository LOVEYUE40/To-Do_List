{
  "//": "solution 文件：只负责把编辑器指向两个子项目，本身不编译任何文件。",
  "//check": "类型检查请用 npm run typecheck（分别跑 node / web 两套配置）。",
  "files": [],
  "references": [{ "path": "./tsconfig.node.json" }, { "path": "./tsconfig.web.json" }]
}
